meta {
  name: create class
  type: http
  seq: 1
}

post {
  url: http://localhost:5018/classes
  body: json
  auth: inherit
}

body:json {
  {
    "className": "string",
    "subject": "string",
    "section": "string",
    "color": "string",
    "ownerId": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
